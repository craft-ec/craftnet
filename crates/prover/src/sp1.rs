// Receipt SP1 prover removed — receipt ZK proving is unnecessary.
// Receipt signatures are verified directly by the aggregator (ed25519).
// Only the distribution Groth16 proof remains for on-chain verification.
